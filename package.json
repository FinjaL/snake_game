{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "3D Snake Game with Leaderboard",
  "main": "game.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "start:dev": "live-server --watch=**/*.js --open=game.html",
    "build": "mkdir -p dist && (cp -r *.html *.js dist/ 2>/dev/null || true)"
  },
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/plugin-transform-modules-commonjs": "^7.27.1",
    "@babel/preset-env": "^7.28.5",
    "@testing-library/jest-dom": "^6.9.1",
    "babel-jest": "^30.2.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "live-server": "^1.2.2"
  }
}
